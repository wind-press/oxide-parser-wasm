name: Publish to Packagist

on:
  push:
    tags:
      - '*'

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Set up Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true

      - name: Build for Linux
        if: ${{ matrix.os == 'ubuntu-latest' }}
        run: |
          rustup target add x86_64-unknown-linux-gnu
          cargo build --release --target x86_64-unknown-linux-gnu

      - name: Build for macOS
        if: ${{ matrix.os == 'macos-latest' }}
        run: |
          rustup target add x86_64-apple-darwin
          cargo build --release --target x86_64-apple-darwin

      - name: Build for Windows
        if: ${{ matrix.os == 'windows-latest' }}
        run: |
          rustup target add x86_64-pc-windows-gnu
          cargo build --release --target x86_64-pc-windows-gnu

      - name: Upload artifact
        run: |
          mkdir -p artifact/php/ext/
          if [ "${{ matrix.os }}" = "ubuntu-latest" ]; then
            cp target/x86_64-unknown-linux-gnu/release/liboxide_parser_wasm.so artifact/php/ext/
          elif [ "${{ matrix.os }}" = "macos-latest" ]; then
            cp target/x86_64-apple-darwin/release/liboxide_parser_wasm.dylib artifact/php/ext/
          elif [ "${{ matrix.os }}" = "windows-latest" ]; then
            mkdir -p artifact\php\ext\
            copy target\x86_64-pc-windows-gnu\release\oxide_parser_wasm.dll artifact\php\ext\
          fi
        shell: bash

      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: build-${{ matrix.os }}
          path: artifact/

  publish:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v3
        with:
          path: collected-artifacts

      - name: Checkout target repository
        uses: actions/checkout@v4
        with:
          repository: wind-press/oxide-parser-php
          ref: main
          path: remote-repository
          token: ${{ secrets.ORG_DEV_PAT }}

      - name: Copy artifacts to target repository
        run: |
          cp -a collected-artifacts/php/. remote-repository/php/ext/

      - name: Setup Git
        run: |
          git config --global user.name "Joshua Gugun Siagian"
          git config --global user.email "suabahasa@gmail.com"

      - name: Commit and tag
        working-directory: remote-repository
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git add --all
          git commit -m "oxide-parser-wasm ${GITHUB_REF#refs/tags/}"
          git push
          git tag ${GITHUB_REF#refs/tags/} -m "${GITHUB_REF#refs/tags/}"
          git push origin ${GITHUB_REF#refs/tags/}
